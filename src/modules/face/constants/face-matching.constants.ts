// Four active enrollment angles can occupy the first four nearest rows. Five results guarantee
// that the matcher can still inspect the closest different driver for the impostor-margin check.
export const FACE_MATCH_CANDIDATE_LIMIT = 5;
